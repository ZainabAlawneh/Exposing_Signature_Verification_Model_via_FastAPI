# Exposing_Signature_Verification_Model_via_FastAPI
Deploy the trained Signet-based signature verification model using FastAPI and build a Python client to interact with it.

#### run uvicorn FastApi:app


#### run python client.py
